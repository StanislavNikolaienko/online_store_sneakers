image: python:3.11.0
variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"
  DJANGO_SECRET_KEY: "django-insecure-)9i*oj8xt86)c-@il)5i#@^@&9(3x3j!_ztxk)654nv6jh#4u2"

before_script:
  - python -m venv venv
  - source venv/bin/activate
  - python -V
  - pip install poetry
  - poetry install --no-root
  - export PATH="/root/.local/bin:$PATH"

cache:
  paths:
    - .cache/pip
    - venv/

stages:
  - linting
  - test

lint_black:
  stage: linting
  script:
    - black --check .

lint_ruff:
  stage: linting
  script:
    - ruff .

test:
    stage: test
    script:
      - python store/manage.py test
